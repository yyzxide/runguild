# RunGuild 作品集交付审计 — 2026-09-14

基线 `c4b4457`。本轮是代码审查、现有测试重跑与交付计划修订，不代表下列应用缺陷已经修复；也不是逐文件安全审计。

## 验证事实

- 本次 `npm test`：构建通过；199 项测试，198 通过、1 跳过、0 失败，耗时约 151 秒。保持 `--test-concurrency=1`，避免内嵌数据库测试并发占满 WSL 内存。
- 跳过项为外部 PostgreSQL 协调测试，需独立 `_test` 库；不允许针对演示数据库执行其中的清空操作。
- 本次 Compose 未发现运行容器，4173/4000 无法连接。不能使用上次服务上线的记录代表当前部署状态。
- 审计开始工作树干净，本地 main 比本地 origin/main 引用领先 15 个提交。本次未 fetch，远端实时状态未核验。
- `REAL_EVALUATION_2026-08-31.md` 有真实模型执行与恢复记录，但首组配对实验中途修过平台、重复次数不足；后一次只有 multi_agent。它们不能证明多 Agent 效率优势。

## 修复与交付优先级

### P1：独立验收标准

生成项目 `/home/sid/runguild-targets/simple-game`（`08ef93e`）测试存在弱断言：

- `test/engine.test.ts:62` 的撞墙测试允许仍为 running；
- `:90` 的难度增长测试允许难度保持原值；
- `:34` 的增长测试未检查吃到食物后的增长；
- `test/interactive.test.ts` 仅覆盖渲染帧，未覆盖计划要求的 controller + engine 得分阈值。

此前 9 项测试通过只能说明这组断言成立，不能证明完整需求被独立验收。此问题是 Agent 产物验收缺口，不是本仓库 198 项测试全都无效。

下一步：在 Builder 执行前冻结外部验收，验证器置于 Builder 无法修改的环境；增加有意破坏关键行为的 mutant 来验证测试有效；Reviewer 模型意见不替代程序性验收。产物 commit、测试版本与验证结果绑定。

### P1：任务入口的可恢复提交

`apps/web/src/App.tsx:939` 先保存消息、清空草稿，再创建 planning request。`apps/web/src/api.ts:903` 的两个请求每次调用都生成新幂等键。第二步失败或响应丢失后的恢复不足。本次属于代码路径发现，未进行浏览器断网复现。

下一步：稳定且可恢复的 client_request_id；服务端原子保存任务消息与规划请求，或使用可恢复命令记录；失败后沿用同一请求身份。验收消息保存后断网、成功响应丢失、刷新、新任务/已完成 Mission 的分流。问候不能无解释地产生代码任务。

### P1：可信执行边界

`packages/workspace-tools/src/workspace-tools.ts:77` 在宿主机 spawn 测试进程。Worktree、shell=false、argv 白名单和精简 env 有价值，但不限制仓库脚本访问宿主文件、网络或派生进程。

个人可信环境可以先明确边界；执行不可信仓库前加入受限非 root 容器、仅挂载 Task Worktree、无凭据/宿主 Docker socket、资源限制、验证阶段断网和进程组取消。依赖准备与最终测试分开。不能宣传完整 OS Sandbox。

### P1：真实评测与未知价格（代码门禁已完成，重复实验待执行）

账本聚合、协议、报告和 Web 已把缺失价格保留为 `null`，并公开
`pricedTrials`、`pairedCostTrials` 与成本完整性限制；迁移 0029 精确修复
底层调用价格缺失的历史 Trial。报告不足三组完整配对时标为探索性，
达到门槛也继续声明未证明统计显著。价格表版本化仍是后续增强项。

拟定小样本：局部 bug、接口实现、可分解跨模块改动三类；同基线与模型配置，单/多 Agent 各重复 3 次，共 18 Trial。该规模只是工程验收建议，未执行且不保证统计显著。报告成功、成本完整性、token、工具失败、返工、人工干预；超时和中途修框架不可当正常结果混算。

### P2：复现与展示

- 添加仓库 CI（目前未发现 `.github`），独立 PostgreSQL 套件与浏览器 E2E；
- 固定启动/检查流程，展示 API/Worker/模型连接的分别就绪状态；
- 一条 5–10 分钟讲得清的演示、一次崩溃恢复、一次拒绝错误产物；
- README 的通用 Runtime 默认 2 次协议纠错与 Worker 配置 5 次分开说明；
- 模型能力固定与条件验证。2026-09-14 DeepSeek 官方说明旧 `deepseek-v4-flash` 别名映射到新 Flash；新实验记录 requested/returned model、时间和配置，不重写历史标签。

官方依据：[DeepSeek 当前模型说明](https://api-docs.deepseek.com/)、[Responses 兼容表](https://api-docs.deepseek.com/guides/responses_api/)。其中 background 和 previous_response_id 不支持、parallel_tool_calls 被忽略；不能把兼容接口当作全部能力一致。

## 定位和收尾估算

保留 TypeScript/PostgreSQL/React 技术栈，突出“持久化协作、证据审查、恢复和交付”，不重写为 Python，不把多人管理和复杂 UI 扩展作为求职前置。

当前阶段：主链已实现，有真实运行记录，尚未达到稳定独立验收的作品集版本。上述核心收尾粗估 5–8 个有效开发日，每日按约 5 小时实现、验证和复盘计算；实际按验收项重新估算。

配套 Voren 负责 Python 单 Agent、邮件/日程外部动作、来源检索与 Skill 学习；共同的 Agent Loop/Harness 知识允许重叠。双项目完整计划位于 `/home/sid/Voren_agent/docs/reviews/2026-09-14-PORTFOLIO-AUDIT.zh-CN.md`。个人简历和求职材料留在本地，不写入本公开仓库。

本轮没有重新启动 live Missions、未清理历史 Worktree、未改变评审结果。下一轮先处理独立验收与任务入口恢复，再进行新的真实模型实验。
